node_modules/
dist/
coverage/
*.log

.DS_Store
.playwright-mcp/

.env
.env.local
.env.*.local

data/

# Group working state is runtime-generated and not version-controlled.
# `groups/` holds live agent workspaces created at runtime by group-init.
# Worked examples that ship with the repo live under `examples/` (tracked),
# not `groups/`.
groups/*

logs/

container/agent-runner/node_modules/

*.sqlite
*.db
*.db-shm
*.db-wal
*.pid

# Agent working state (advisory, not authoritative — see CLAUDE.md §Session Continuation Guide)
.sisyphus/

